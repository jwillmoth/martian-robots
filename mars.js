'use strict';

class Mars {
    constructor(height, width) {
        this.height = height;
        this.width = width;
        this.scentedPositions = new Array();
    }

    isOffGrid(x, y) {

    }

    isScented(x, y) {
        //add a 
    }

    addScented(x, y) {

    }
}

module.exports = Mars;