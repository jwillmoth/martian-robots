'use strict';

class Mars {
    constructor(maxX, maxY) {
        this.maxX = maxX;
        this.maxY = maxY;
        this.scentedPositions = new Array();
    }

    isOffGrid(x, y) {

    }

    isScented(x, y) {
        //add a 
    }

    addScented(x, y) {

    }
}

module.exports = Mars;